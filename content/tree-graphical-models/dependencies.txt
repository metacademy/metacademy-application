tag: bayesian-networks
reason: Trees are a case where Bayes nets and MRFs are equivalent.

tag: markov-random-fields
reason: Trees are a case where Bayes nets and MRFs are equivalent.

tag: conditional-independencies
reason: Trees can be thought of in terms of conditional independencies.
